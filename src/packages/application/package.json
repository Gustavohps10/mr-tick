{
  "name": "@mr-tick/application",
  "version": "1.0.1",
  "private": true,
  "description": "Contém os serviços e casos de uso (use cases) da aplicação Mr-tick.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup --format esm,cjs --dts",
    "dev": "tsup --format esm,cjs --watch --no-dts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@mr-tick/domain": "workspace:*",
    "@mr-tick/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
